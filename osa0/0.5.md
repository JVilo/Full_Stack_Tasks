```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Käyttäjä kirjoittaa uuden muistiinpanon SPA:ssa

    %% 1. SPA ei lataa sivua uudestaan
    Note right of browser: Sivu on jo ladattu aiemmin (HTML + JS)

    %% 2. AJAX POST
    browser->>server: POST /exampleapp/new_note_spa
    Note left of server: Palvelin tallentaa muistiinpanon
    server-->>browser: 201 Created (JSON vastaus)

    %% 3. UI päivitys JS:llä
    Note right of browser: JavaScript lisää uuden muistiinpanon DOM:iin\nilman sivun uudelleenlatausta